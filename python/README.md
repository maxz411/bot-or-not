# bot-or-not (python)

Python utilities for:

- evaluation helpers
- v5 fine-tuning data prep/training/eval orchestration (`fine_tuning` package)

Main CLI:

```bash
python3 -m fine_tuning --help
```

